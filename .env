# Models to load (comma-separated IDs)
MODELS=1

# Default model for all prediction endpoints
DEFAULT_MODEL=1

# Model IDs:
# 1 = YOLOv8_Small_RDD (PyTorch)
# 2 = YOLO_TFRT_16 (TensorRT FP16) - requires GPU
# 3 = YOLO_TFRT_32 (TensorRT FP32) - requires GPU
# 4 = YOLO_TFLITE_16 (TFLite FP16)
# 5 = YOLO_TFLITE_32 (TFLite FP32)